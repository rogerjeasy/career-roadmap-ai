import { apiClient } from "./client";

// Backend routes: GET /api/v1/progress  POST /api/v1/progress/review

export const progressApi = {
  // placeholder — implement when progress domain is ready
};
