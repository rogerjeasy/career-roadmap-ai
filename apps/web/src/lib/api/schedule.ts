import { apiClient } from "./client";

// Backend routes: GET /api/v1/schedule  POST /api/v1/schedule/habits

export const scheduleApi = {
  // placeholder — implement when schedule domain is ready
};
