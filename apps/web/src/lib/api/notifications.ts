import { apiClient } from "./client";

// Backend routes: GET /api/v1/notifications  PATCH /api/v1/notifications/:id/read

export const notificationsApi = {
  // placeholder — implement when notifications domain is ready
};
