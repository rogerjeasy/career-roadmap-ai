import { apiClient } from "./client";

// Backend routes: WS /ws/agent-stream/:session  POST /api/v1/coach/message

export const coachApi = {
  // placeholder — implement when coach/streaming domain is ready
};
