"""Analytics domain — cross-feature insight dashboard.

Aggregates the data the user has already generated across the platform —
applications funnel, learning ROI, wellness trend, evidence/portfolio/credential
counts, and momentum (weekly reviews) — into one read-only overview. No new data
is created; this is a projection over existing owner-scoped collections.
"""
