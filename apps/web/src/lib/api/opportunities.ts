import { apiClient } from "./client";

// Backend routes: GET /api/v1/opportunities  GET /api/v1/opportunities/:id

export const opportunitiesApi = {
  // placeholder — implement when opportunities domain is ready
};
