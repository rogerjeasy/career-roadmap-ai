import { apiClient } from "./client";

// Backend routes: GET /api/v1/networking/contacts  POST /api/v1/networking/outreach

export const networkingApi = {
  // placeholder — implement when networking domain is ready
};
